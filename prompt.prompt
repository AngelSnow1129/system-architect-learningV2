1、对项目下的内容进行更新，将所有md的文件名称去除多余的内容和时间戳，并且序号需要为两位1-9前面增加0的前缀；
2、项目中的插入图片时，复制到指定路径 ./images/${filename}(其中`${filename}`表示当前文件名)
3、对图片名称进行相应的格式化按照序号来进行命名，命名格式为：${filename}-${index}.png (其中`${index}`表示当前图片的序号，从1开始)
4、请直接使用rm等shell进行清除，现在手动将不需要的图片在md中清除了，请手动清除多余的images中没有使用的图片
5、请将项目下的所有的md文件，将文档内的内容进行修复和修缮错误的错别字等内容，



